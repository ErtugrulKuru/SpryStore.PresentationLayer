@model List<Product>
@{
    ViewData["Title"] = "GetLast4Product";
    Layout = "~/Views/AdminLayout/Index.cshtml";
    int count = 0;
}

<h1>Son Eklenen 4 Ürün</h1>

<table class="table table-hover">
    <tr>
        <th>#</th>
        <th>Ürün Adı</th>
        <th>Ürün Fiyatı</th>
    </tr>
    @foreach (var item in Model)
    {
        count++;
        <tr>
            <td>@item.ProductID</td>
            <td>@item.ProductName</td>
            <td>@item.Price</td>
        </tr>
    }
</table>

